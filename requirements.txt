fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pytest>=8.0.0
httpx>=0.27.0
streamlit>=1.28.0
plotly>=5.18.0
requests>=2.31.0
matplotlib>=3.8.0
python-multipart>=0.0.6
jupyter
nbconvert
# OpenEnv CLI is validated via local toolchain gates, not installed in HF Space runtime.
# Keep runtime dependencies minimal for reliable Space builds.
# Heavy ML dependencies (install in Colab only — see grpo_colab_v2.ipynb Cell 1)
# trl>=0.8.0
# unsloth
# transformers>=4.40.0
# torch>=2.2.0
# xformers (optional, will fail in some environments)