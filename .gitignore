# --- Secrets / local env ---
.env
.env.*
!.env.example

# --- Python bytecode & packaging ---
*.py[cod]
*$py.class
__pycache__/
*.so
.Python
*.egg
*.egg-info/
.eggs/
dist/
build/
pip-wheel-metadata/

# --- Virtual environments ---
venv/
.venv/

# --- Test & type / lint caches ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
nosetests.xml
coverage.xml
*.cover
.hypothesis/

# --- Jupyter ---
.ipynb_checkpoints/
*.ipynb_checkpoints

# --- OS / editors ---
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo
*~

# --- Project lockfile (repo policy: do not commit uv.lock) ---
uv.lock


# --- Training / RL artifacts (regenerate locally) ---
training_artifacts/pre_event_reward_curves.json
# Local training output at repo root only (not Colab backup checkpoints under NEXUS_GRPO_backups/)
/grpo_checkpoints/
wandb/
episode_rewards.json
*.png
!docs/images/training_reward_curve.png
!NEXUS_GRPO_backups/**/*.png
!web/training_runs/**/*.png
# Colab backup adapter weights are large; keep JSON/plots/config in git
NEXUS_GRPO_backups/**/grpo_checkpoints/**/*.safetensors

# --- HF / hub local caches ---
.cache/
.huggingface/

# --- Optional frontend tooling ---
node_modules/

# --- Logs ---
*.log
logs/

# --- Legacy / duplicate UIs (canonical: streamlit_app_v2.py, gradio_app.py) ---
streamlit_app.py
gradio_app_backup.py
gradio_app_minimal.py

# --- Local assistant / scratch notes (optional) ---
CLAUDE.md
docs/meta/CLAUDE.md
